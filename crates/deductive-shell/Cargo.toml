[package]
name = "deductive-shell"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "deductive-shell"
path = "src/main.rs"

[dependencies]
refactor-check-core = { path = "../core" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
